<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

<div class="news-container" style="margin-top: 20px">
    <h1>Club News & Updates</h1>
    <div class="news-list">
        <% posts.forEach(post=> { %>
        <div class="news-card">
            <% if (post.thumbnail) { %>
            <div class="thumb-wrapper">
                <div class="thumb-parallax" style="background-image: url('<%= post.thumbnail %>');">
                </div>
            </div>
            <% } %>

            <h3>
                <a href="/news/<%= post.id %>">
                    <%= post.title %>
                </a>
            </h3>

            <div class="author-block">
                <% if (post.authorThumb) { %>
                <img class="author-thumb" src="<%= post.authorThumb %>" alt="<%= post.author %>">
                <% } %>

                <div class="author-info">
                    <span class="news-author">
                        <%= post.author %>
                    </span>
                    <span class="news-date">
                        <%= post.date.toLocaleString("en-US", {year: "numeric" ,month: "long",day: "numeric" ,hour: "2-digit" ,minute: "2-digit" }); %>
                    </span>
                </div>
            </div>

            <div class="news-tags">
                <% post.tags.forEach(tag=> { %>
                <span class="tag">
                    <%= tag %>
                </span>
                <% }) %>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<button id="openCreateMenu" class="create-news-btn">Create Post</button>
<div id="createMenuOverlay" class="create-menu-overlay"></div>

<div id="createMenu" class="create-menu">
    <h2>Create News Post</h2>
    <button id="closeCreateMenu" class="close-menu-btn">×</button>

    <form action="/news" method="POST" class="create-form">
        <label>
            Title
            <input type="text" name="title" required>
        </label>

        <label>
            Thumbnail URL
            <input type="text" name="thumbnail" placeholder="https://example.com/image.jpg">
        </label>

        <label>
            Tags
            <input type="text" name="tags" placeholder="club, update, event">
        </label>

        <label>
            Content
            <textarea name="content" rows="10" required></textarea>
        </label>

        <button type="submit" class="submit-btn">Publish</button>
    </form>
</div>

<link rel="stylesheet" href="/styles/pages/news.css">
<script src="/scripts/newsParallax.js"></script>
<script src="/scripts/newsCreation.js"></script>